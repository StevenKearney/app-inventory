# app-inventory
An all-in-one Linux system inventory tool that scans all major installation sources. Optionally outputs a complete list of applications, packages, containers, and models in TSV/CSV/JSON formats. Includes a simple search feature.
